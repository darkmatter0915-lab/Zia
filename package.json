{
  "name": "zia-personal-os",
  "private": true,
  "version": "1.0.0",
  "description": "Local-first personal control center for tasks, restaurant operations, travel, investments, vehicles, games and notes.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "check": "node scripts/check.mjs",
    "build": "npm run check && vite build",
    "preview": "vite preview",
    "predev": "node scripts/prepare-lab.mjs",
    "prebuild": "node scripts/prepare-lab.mjs",
    "test": "node --test tests/unit/*.test.js",
    "test:browser": "playwright test",
    "verify:dist": "node scripts/verify-lab-build.mjs"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "devDependencies": {
    "vite": "^7.1.0",
    "@playwright/test": "1.58.2"
  },
  "dependencies": {
    "three": "0.185.1"
  }
}
