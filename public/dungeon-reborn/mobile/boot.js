(async()=>{const fail=(m)=>{const box=document.createElement('main');const text=document.createElement('div');text.textContent='載入失敗：'+m;box.append(text);document.body.replaceChildren(box)};try{const packed=window.__DR_GZ||'';if(!packed)throw new Error('遊戲資料未完整載入');if(!('DecompressionStream'in window))throw new Error('請使用最新版 Safari 或 Chrome');const binary=atob(packed);const bytes=new Uint8Array(binary.length);for(let i=0;i<binary.length;i++)bytes[i]=binary.charCodeAt(i);const stream=new Blob([bytes]).stream().pipeThrough(new DecompressionStream('gzip'));const html=await new Response(stream).text();window.__DR_GZ='';const frame=document.createElement('iframe');frame.title='地下城重生';frame.allow='fullscreen';frame.style.cssText='position:fixed;inset:0;width:100%;height:100%;border:0;background:#08050d';frame.srcdoc=html;document.body.replaceChildren(frame)}catch(error){fail(String(error&&error.message||error))}})();