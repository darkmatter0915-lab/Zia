(async()=>{const showError=(message)=>{const main=document.querySelector('main');if(main)main.innerHTML='';const box=document.createElement('div');box.style.cssText='height:100%;display:grid;place-items:center;text-align:center;color:#f2bd55';const text=document.createElement('div');text.textContent='載入失敗：'+message;box.append(text);document.body.replaceChildren(box)};try{const packed=window.__DR_GZ||'';if(!packed)throw new Error('遊戲資料未載入');const binary=atob(packed);const bytes=new Uint8Array(binary.length);for(let i=0;i<binary.length;i++)bytes[i]=binary.charCodeAt(i);if(!('DecompressionStream' in window))throw new Error('請更新 Safari 或 Chrome');const decodedStream=new Blob([bytes]).stream().pipeThrough(new DecompressionStream('gzip'));const html=await new Response(decodedStream).text();window.__DR_GZ='';const frame=document.createElement('iframe');frame.title='地下城重生';frame.allow='fullscreen';frame.style.cssText='position:fixed;inset:0;width:100%;height:100%;border:0;background:#08050d';frame.srcdoc=html;document.body.replaceChildren(frame)}catch(error){showError(String(error&&error.message||error))}})();