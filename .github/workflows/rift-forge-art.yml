name: Rift Forge combat art verification
on:
  push:
    branches: [main, rift-forge-combat-art-v03, rift-forge-mobile-v04]
    paths: ['public/rift-forge/**', 'public/sw.js', 'scripts/rift-forge/**', 'tests/fixtures/zia-sw-before-rift.js', 'package.json', 'playwright.rift.config.cjs', '.github/workflows/rift-forge-art.yml']
  pull_request:
    paths: ['public/rift-forge/**', 'public/sw.js', 'scripts/rift-forge/**', 'tests/fixtures/zia-sw-before-rift.js', 'package.json', 'playwright.rift.config.cjs', '.github/workflows/rift-forge-art.yml']
permissions:
  contents: read
jobs:
  combat-art:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: node --test public/rift-forge/tests/engine.test.cjs
      - run: npm run build
      - run: npx playwright install --with-deps chromium webkit
      - run: npx playwright test --config=playwright.rift.config.cjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: rift-forge-v04-evidence
          path: test-results/
          retention-days: 14
